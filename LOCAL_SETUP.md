# Локальная установка для тестирования

## Быстрый старт

### 1. Клонируй репозиторий
```bash
git clone <ваш_репозиторий>
cd ботик
```

### 2. Создай виртуальное окружение
```bash
python3 -m venv venv
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate     # Windows
```

### 3. Установи зависимости
```bash
pip install -r requirements.txt
```

### 4. Настрой .env с ТВОИМ токеном
```bash
cp .env.example .env
nano .env  # или любой редактор
```

**Обязательно укажи свой `BOT_TOKEN`:**
```env
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz  # Твой токен от @BotFather
ADMIN_ID=123456789                              # Твой Telegram ID
LOG_LEVEL=DEBUG                                 # Для отладки
```

### 5. Запусти бота
```bash
python -m schedule_bot.bot
```

## Что произойдет при первом запуске

1. ✅ **БД создастся автоматически** — `schedule_bot/schedule.db`
2. ✅ **Папка кэша создастся** — `schedule_bot/schedule_data/`
3. ✅ **Загрузятся данные о сессии** — из папки `schedule_bot/сессия/`
4. ✅ **Проиндексируются файлы зачетов/экзаменов** — из папки `зачеты и сессия/`

## Тестирование

### Проверь основное расписание
1. Отправь `/start` боту
2. Введи группу (например: `06-451`)
3. Нажми "Расписание"
4. Выбери день недели

### Проверь зачеты/экзамены
1. Нажми "Зачеты" — должно показать расписание зачетов
2. Нажми "Экзамены" — должно показать расписание экзаменов

### Проверь админку (если указал ADMIN_ID)
1. `/admin` — меню
2. `/stats` — статистика
3. `/users` — список групп

## Что НЕ попадет в Git

Благодаря `.gitignore`:
- ❌ `schedule_bot/schedule.db` — твоя локальная БД
- ❌ `schedule_bot/schedule_data/*.xlsx` — кэш файлов
- ❌ `.env` — твой токен
- ❌ `__pycache__/` — скомпилированные файлы

## Остановка бота

```bash
# Ctrl+C в терминале
# или
kill <PID>
```

## Очистка для повторного теста

```bash
# Удалить БД и кэш
rm schedule_bot/schedule.db
rm schedule_bot/schedule_data/*.xlsx

# Запустить снова
python -m schedule_bot.bot
```

## Переключение на другой API

### Если хочешь тестировать с другим ботом:

1. Создай нового бота у @BotFather
2. Получи новый токен
3. Измени в `.env`:
```env
BOT_TOKEN=новый_токен_здесь
```
4. Перезапусти бота

**БД и кэш остаются локально** — ничего не загружается в Git!

## Проблемы?

### БД создается но пустая
✅ Нормально! Пользователи добавятся после `/start`

### Нет расписания зачетов/экзаменов
✅ Проверь что файлы лежат в `зачеты и сессия/`

### Ошибка "BOT_TOKEN is not set"
✅ Скопировал `.env.example`? Заполнил `BOT_TOKEN`?

### Не показывает расписание
✅ Проверь что сайт КФУ доступен
✅ Проверь логи в консоли (поставь `LOG_LEVEL=DEBUG`)

## Готово! 🚀

Теперь можешь тестировать с любым токеном, а БД останется только у тебя локально.

